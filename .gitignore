# macOS
.DS_Store
._*

# local tool state (keep launch.json, it documents how to run the dev server)
.claude/settings.local.json

# scratch
*.log
node_modules/
# Excel writes a ~$ lock file next to any open workbook. It is 165 bytes of
# nothing and it got committed once already.
~$*
.DS_Store

# Carrier name mapping. Masked in the build per Anshul; this must not be committed.
carrier-names.local.json

# Figma timeline export, kept out of the build until the real loop lands
build/media/attract-v4-figma.mp4

# 28MB of source PNGs for the V4 renders. The build ships the JPEGs.
artwork-src/
